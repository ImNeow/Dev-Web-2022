import { Row, Col , Container } from "react-bootstrap"

const Accueil = () => {
    return (
        <div class="Administration">
            Administration
        </div>
    );
 }
export default Accueil
