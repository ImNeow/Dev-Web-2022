import { Row, Col , Container } from "react-bootstrap"

const userHistory = () => {
    return (
        <div className="userHistory">
            user History
        </div>
    );
 }
export default userHistory
